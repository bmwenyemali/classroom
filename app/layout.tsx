import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Classroom - Student Management System",
    template: "%s | Classroom",
  },
  description:
    "A comprehensive classroom management system for students, teachers, and administrators. Manage courses, grades, enrollments, and academic performance.",
  keywords: [
    "classroom",
    "education",
    "student management",
    "grades",
    "courses",
    "academic",
  ],
  authors: [{ name: "Classroom Team" }],
  icons: {
    icon: "/icon.svg",
    apple: "/icon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
